# 🧪 Повний план налаштування та тестування Auth функціоналу

## 📋 Зміст

1. [Передумови](#передумови)
2. [Налаштування сервера](#налаштування-сервера)
3. [Налаштування мобільного додатку](#налаштування-мобільного-додатку)
4. [Запуск та тестування](#запуск-та-тестування)
5. [Чек-лист тестування](#чек-лист-тестування)
6. [Типові проблеми та рішення](#типові-проблеми-та-рішення)

---

## 🔧 Передумови

### Необхідне програмне забезпечення:

- ✅ Node.js >= 18.0.0
- ✅ MongoDB (локально або Atlas)
- ✅ React Native CLI
- ✅ Android Studio (для Android емулятора)
- ✅ Xcode (для iOS симулятора)
- ✅ Git

### Перевірка встановлення:

```bash
node --version  # Має бути >= 18.0.0
npm --version
mongod --version
```

---

## 🖥️ Налаштування сервера

### Крок 1: Створення .env файлу

Створіть файл `server/.env` з наступним вмістом:

```env
NODE_ENV=development
PORT=3000
MONGODB_URI=mongodb://localhost:27017/smartcoach
JWT_SECRET=your-super-secret-key-minimum-32-characters-long-for-security-change-this
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d
SMS_PROVIDER=twilio
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
CORS_ORIGIN=http://localhost:3000,http://10.0.2.2:3000,http://127.0.0.1:3000
LOG_LEVEL=info
```

**Важливо:**

- `JWT_SECRET` - змініть на випадковий рядок мінімум 32 символи
- `CORS_ORIGIN` - містить адреси для iOS (localhost) та Android (10.0.2.2) емуляторів
- `MONGODB_URI` - якщо використовуєте MongoDB Atlas, замініть на ваш connection string

### Крок 2: Налаштування MongoDB

**⚠️ ВАЖЛИВО:** У вас вже налаштований MongoDB Atlas (хмарна версія)!

Якщо ви використовуєте **MongoDB Atlas** (як у вашому випадку):

- ✅ **НЕ потрібно** встановлювати або запускати локальний MongoDB
- ✅ MongoDB Compass - це просто GUI клієнт для перегляду даних
- ✅ Сервер автоматично підключиться до Atlas при запуску
- ✅ Перевірте, що `MONGODB_URI` в `.env` містить правильний connection string

**Якщо хочете використовувати локальний MongoDB** (замість Atlas):

**macOS (Homebrew):**

```bash
brew tap mongodb/brew
brew install mongodb-community
brew services start mongodb-community
# Перевірка статусу
brew services list | grep mongodb
```

**Linux:**

```bash
sudo systemctl start mongod
# Або
sudo service mongod start
```

**Windows:**

```bash
net start MongoDB
```

**Перевірка підключення (тільки для локального MongoDB):**

```bash
mongosh mongodb://localhost:27017/smartcoach
```

### Крок 3: Встановлення залежностей сервера

```bash
cd server
npm install
```

### Крок 4: Збірка TypeScript (якщо потрібно)

```bash
cd server
npm run build
```

### Крок 5: Запуск сервера

**Development режим (з auto-reload):**

```bash
cd server
npm run dev
```

**Production режим:**

```bash
cd server
npm run build
npm start
```

**Очікуваний вивід:**

```
🚀 Сервер запущено на порту 3000
📝 Environment: development
```

### Крок 6: Перевірка роботи сервера

Відкрийте новий термінал та виконайте:

```bash
curl http://localhost:3000/health
```

**Очікувана відповідь:**

```json
{
  "success": true,
  "message": "Server is running",
  "timestamp": "2024-..."
}
```

---

## 📱 Налаштування мобільного додатку

### Важливо: API URL для різних платформ

| Платформа             | API URL                                          |
| --------------------- | ------------------------------------------------ |
| **iOS Simulator**     | `http://localhost:3000` ✅                       |
| **Android Emulator**  | `http://10.0.2.2:3000` ✅                        |
| **Фізичний пристрій** | `http://192.168.x.x:3000` (IP вашого комп'ютера) |

### Поточна конфігурація

Наразі API URL налаштований в наступних файлах:

- ✅ `src/screens/ForgotPasswordScreen.js`
- ✅ `src/screens/ForgotPasswordCodeScreen.js`
- ✅ `src/screens/ResetPasswordScreen.js`

**Приклад налаштування:**

```javascript
import {Platform} from 'react-native';

const API_BASE_URL = __DEV__
  ? Platform.OS === 'android'
    ? 'http://10.0.2.2:3000'
    : 'http://localhost:3000'
  : 'https://your-production-api.com';
```

### Встановлення залежностей клієнта

```bash
# В кореневій папці проекту
npm install
```

### Для iOS (додатково):

```bash
cd ios
pod install
cd ..
```

---

## 🚀 Запуск та тестування

### Варіант 1: Android емулятор

#### 1. Запуск Android емулятора

```bash
# Відкрийте Android Studio та запустіть емулятор
# Або через командний рядок:
emulator -avd <AVD_NAME>
```

#### 2. Запуск Metro Bundler

```bash
# В кореневій папці проекту
npm start
```

#### 3. Запуск додатку на Android

```bash
npm run android
```

**Перевірка підключення:**

- Сервер має бути запущений на `http://localhost:3000`
- Додаток автоматично підключиться до `http://10.0.2.2:3000`

### Варіант 2: iOS симулятор

#### 1. Запуск iOS симулятора

```bash
# Відкрийте Xcode та запустіть симулятор
# Або через командний рядок:
open -a Simulator
```

#### 2. Запуск Metro Bundler

```bash
npm start
```

#### 3. Запуск додатку на iOS

```bash
npm run ios
```

**Перевірка підключення:**

- Сервер має бути запущений на `http://localhost:3000`
- Додаток автоматично підключиться до `http://localhost:3000`

### Варіант 3: Фізичний пристрій

#### 1. Знайдіть IP адресу вашого комп'ютера

**macOS/Linux:**

```bash
ifconfig | grep "inet " | grep -v 127.0.0.1
# Або
ipconfig getifaddr en0
```

**Windows:**

```bash
ipconfig
# Шукайте IPv4 адресу (наприклад, 192.168.1.100)
```

#### 2. Оновіть API_BASE_URL в екранах

Замініть `localhost` або `10.0.2.2` на ваш IP адрес:

```javascript
const API_BASE_URL = __DEV__
  ? 'http://192.168.1.100:3000' // Ваш IP
  : 'https://your-production-api.com';
```

#### 3. Переконайтесь, що пристрій та комп'ютер в одній мережі Wi-Fi

#### 4. Запустіть додаток

```bash
npm run android  # або npm run ios
```

---

## ✅ Чек-лист тестування

### Тестування відновлення паролю

#### 1. Забули пароль (ForgotPasswordScreen)

- [ ] Відкрити екран "Забули пароль?"
- [ ] Ввести номер телефону (формат: +38 XXX XXX XX XX)
- [ ] Натиснути "Надіслати SMS"
- [ ] Перевірити, що код відображається в консолі сервера (development режим)
- [ ] Перевірити, що відбувається перехід на екран введення коду
- [ ] Перевірити обробку помилок (неправильний номер, мережева помилка)

#### 2. Введення коду (ForgotPasswordCodeScreen)

- [ ] Ввести код з консолі сервера
- [ ] Перевірити валідацію коду
- [ ] Перевірити таймер повторного відправлення
- [ ] Перевірити обробку невірного коду
- [ ] Перевірити перехід на екран створення нового паролю

#### 3. Створення нового паролю (ResetPasswordScreen)

- [ ] Ввести новий пароль
- [ ] Ввести підтвердження паролю
- [ ] Перевірити валідацію паролю (мінімум 8 символів, великі/малі літери, цифри)
- [ ] Перевірити, що паролі співпадають
- [ ] Натиснути "Змінити пароль"
- [ ] Перевірити успішний перехід на екран входу

### Тестування реєстрації (якщо інтегровано)

#### 1. Реєстрація (RegistrationScreen)

- [ ] Ввести номер телефону
- [ ] Натиснути "Отримати SMS"
- [ ] Перевірити API виклик в консолі сервера
- [ ] Перевірити перехід на екран введення коду

#### 2. Введення коду (RegistrationCodeScreen)

- [ ] Ввести код з консолі сервера
- [ ] Перевірити валідацію
- [ ] Перевірити перехід на екран створення паролю

#### 3. Створення паролю (CreatePasswordScreen)

- [ ] Ввести пароль
- [ ] Ввести підтвердження паролю
- [ ] Перевірити валідацію
- [ ] Перевірити перехід на екран входу

### Тестування входу (якщо інтегровано)

#### 1. Вхід (LoginScreen)

- [ ] Ввести номер телефону
- [ ] Ввести пароль
- [ ] Натиснути "Увійти"
- [ ] Перевірити API виклик
- [ ] Перевірити збереження токенів
- [ ] Перевірити перехід на головний екран

---

## 🔍 Перевірка в MongoDB

### Підключення до БД

```bash
mongosh mongodb://localhost:27017/smartcoach
```

### Перегляд даних

```javascript
// Перегляд користувачів
db.users.find().pretty();

// Перегляд кодів верифікації
db.verificationcodes.find().pretty();

// Перегляд refresh токенів
db.refreshtokens.find().pretty();

// Очищення тестових даних (обережно!)
db.users.deleteMany({});
db.verificationcodes.deleteMany({});
db.refreshtokens.deleteMany({});
```

---

## 🐛 Типові проблеми та рішення

### Проблема 1: "Network request failed" на Android

**Причина:** Android емулятор не може доступитися до `localhost`

**Рішення:**

- Переконайтесь, що використовується `http://10.0.2.2:3000` для Android
- Перевірте, що сервер запущений на порту 3000
- Перевірте налаштування CORS в `.env` файлі

### Проблема 2: CORS помилки

**Причина:** Сервер не дозволяє запити з мобільного додатку

**Рішення:**

1. Перевірте `CORS_ORIGIN` в `server/.env`:
   ```env
   CORS_ORIGIN=http://localhost:3000,http://10.0.2.2:3000,http://127.0.0.1:3000
   ```
2. Перезапустіть сервер після зміни `.env`

### Проблема 3: MongoDB не підключається

**Причина:**

- Якщо використовуєте **MongoDB Atlas**: неправильний connection string або проблеми з мережею
- Якщо використовуєте **локальний MongoDB**: сервер не запущений

**Рішення для MongoDB Atlas:**

1. Перевірте `MONGODB_URI` в `server/.env` - має бути у форматі `mongodb+srv://...`
2. Перевірте, що пароль правильний (якщо містить спецсимволи, закодуйте їх)
3. Перевірте, що IP адреса додана до whitelist в Atlas (0.0.0.0/0 для тестування)

**Рішення для локального MongoDB:**

```bash
# Перевірте статус MongoDB
brew services list | grep mongodb  # macOS
sudo systemctl status mongod       # Linux

# Запустіть MongoDB
brew services start mongodb-community  # macOS
sudo systemctl start mongod            # Linux
```

### Проблема 4: "JWT_SECRET має бути мінімум 32 символи"

**Рішення:**
Змініть `JWT_SECRET` в `server/.env` на рядок >= 32 символи:

```env
JWT_SECRET=your-super-secret-key-minimum-32-characters-long-change-this-now
```

### Проблема 5: Rate limiting помилки

**Причина:** Забагато запитів з одного IP

**Рішення:**

- Зачекайте 15 хвилин
- Або змініть номер телефону для тестування
- Або тимчасово збільште ліміти в `.env`:
  ```env
  RATE_LIMIT_WINDOW_MS=60000
  RATE_LIMIT_MAX_REQUESTS=1000
  ```

### Проблема 6: SMS код не приходить

**Причина:** Twilio не налаштований (в development режимі код виводиться в консоль)

**Рішення:**

- Перевірте консоль сервера - код має виводитися там
- В development режимі SMS не відправляється реально
- Для production налаштуйте Twilio credentials в `.env`

### Проблема 7: Помилка "Cannot connect to Metro"

**Рішення:**

```bash
# Очистка кешу
npm start -- --reset-cache

# Або
watchman watch-del-all  # macOS
rm -rf node_modules
npm install
```

### Проблема 8: Помилки під час збірки iOS

**Рішення:**

```bash
cd ios
pod deintegrate
pod install
cd ..
npm run ios
```

---

## 📊 Моніторинг та дебагінг

### Перевірка логів сервера

Сервер виводить детальні логи всіх запитів. Перевіряйте консоль сервера для:

- Вхідних запитів
- Помилок валідації
- SMS кодів (в development режимі)
- Помилок БД

### Перевірка мережевих запитів в додатку

**React Native Debugger:**

1. Відкрийте меню додатку (shake device або Cmd+D в симуляторі)
2. Виберіть "Debug"
3. Відкрийте Network tab в DevTools

**Альтернатива - додати console.log:**

```javascript
console.log('API Request:', url, body);
console.log('API Response:', response);
```

### Перевірка токенів

Після успішного входу/реєстрації перевірте:

- Чи зберігаються токени в AsyncStorage
- Чи токени передаються в заголовках наступних запитів

---

## 🎯 Наступні кроки

Після успішного тестування:

1. **Інтеграція LoginScreen** - замінити мокові дані на реальні API виклики
2. **Інтеграція RegistrationScreen** - додати API виклики для реєстрації
3. **Збереження токенів** - реалізувати збереження access/refresh токенів
4. **Auto-refresh токенів** - додати автоматичне оновлення токенів
5. **Error handling** - покращити обробку помилок
6. **Loading states** - додати індикатори завантаження

---

## 📝 Нотатки

- В development режимі SMS коди виводяться в консоль сервера
- Для тестування використовуйте реальні номери телефонів (формат: +380XXXXXXXXX)
- Rate limiting може обмежувати кількість спроб - враховуйте це при тестуванні
- MongoDB зберігає дані локально - очищайте БД між тестами якщо потрібно

---

**Успішного тестування! 🚀**
