import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

export const LayoutComponent = () => {
  return (
    <View>
      <Text>LayoutComponent</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
