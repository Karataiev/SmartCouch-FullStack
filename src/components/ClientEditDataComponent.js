import {StyleSheet, Text, View} from 'react-native';

export const ClientEditDataComponent = () => {
  return (
    <View>
      <Text>ClientEditDataComponent</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
