# 🧪 Тестування повного циклу: Реєстрація → Вхід → Відновлення паролю

## ✅ Що інтегровано

Всі екрани тепер інтегровані з API:

- ✅ **RegistrationScreen** - `/api/v1/auth/register`
- ✅ **RegistrationCodeScreen** - `/api/v1/auth/verify-code`
- ✅ **CreatePasswordScreen** - `/api/v1/auth/create-password`
- ✅ **LoginScreen** - `/api/v1/auth/login`
- ✅ **ForgotPasswordScreen** - `/api/v1/auth/forgot-password`
- ✅ **ForgotPasswordCodeScreen** - `/api/v1/auth/verify-code`
- ✅ **ResetPasswordScreen** - `/api/v1/auth/create-password`

---

## 🚀 Передумови

1. **Сервер запущений:**
   ```bash
   cd server
   npm run dev
   ```

2. **MongoDB підключений** (Atlas або локальний)

3. **Мобільний додаток запущений:**
   ```bash
   npm run android  # або npm run ios
   ```

---

## 📋 Покрокове тестування

### 🔵 Крок 1: Реєстрація нового користувача

#### 1.1. Відкрийте екран реєстрації
- Відкрийте додаток
- Натисніть "Зареєструватися" на екрані входу

#### 1.2. Введіть номер телефону
- Введіть номер у форматі: `+38 XXX XXX XX XX`
- Наприклад: `+38 098 123 45 67`
- Натисніть "Отримати SMS"

**Що перевірити:**
- ✅ Кнопка активується тільки коли номер введено правильно (17 символів)
- ✅ Показується індикатор завантаження "Відправка..."
- ✅ Перехід на екран введення коду

**В консолі сервера має з'явитися:**
```
==================================================
📱 SMS КОД ВЕРИФІКАЦІЇ
📞 Номер: +380981234567
🔑 Код: 123456
==================================================
```

#### 1.3. Введіть код верифікації
- Введіть 6-значний код з консолі сервера
- Натисніть "Далі"

**Що перевірити:**
- ✅ Кнопка активується тільки коли введено всі 6 цифр
- ✅ Показується індикатор завантаження "Перевірка..."
- ✅ При невірному коді показується помилка
- ✅ При правильному коді - перехід на екран створення паролю

**Якщо код невірний:**
- Спробуйте ще раз (максимум 3 спроби)
- Або натисніть "Надіслати SMS з кодом" після закінчення таймера

#### 1.4. Створіть пароль
- Введіть пароль (мінімум 8 символів, великі/малі літери, цифри)
- Підтвердіть пароль
- Натисніть "Створити акаунт"

**Вимоги до паролю:**
- Мінімум 8 символів
- Хоча б одна велика літера
- Хоча б одна мала літера
- Хоча б одна цифра

**Що перевірити:**
- ✅ Валідація паролю на клієнті
- ✅ Перевірка співпадіння паролів
- ✅ Показується індикатор завантаження "Створення..."
- ✅ Успішний перехід на екран входу

---

### 🟢 Крок 2: Вхід в систему

#### 2.1. Введіть дані для входу
- Введіть номер телефону (той самий, що при реєстрації)
- Введіть пароль (той самий, що при створенні)
- Натисніть "Увійти"

**Що перевірити:**
- ✅ Кнопка активується тільки коли введено номер та пароль
- ✅ Показується індикатор завантаження "Вхід..."
- ✅ При невірних даних показується помилка
- ✅ При правильних даних - перехід на головний екран (TabBar)

**Токени зберігаються автоматично:**
- `accessToken` - для авторизованих запитів
- `refreshToken` - для оновлення токену

---

### 🔴 Крок 3: Відновлення паролю

#### 3.1. Відкрийте екран відновлення паролю
- На екрані входу натисніть "Забули пароль?"

#### 3.2. Введіть номер телефону
- Введіть номер телефону (той самий, що при реєстрації)
- Натисніть "Надіслати SMS"

**Що перевірити:**
- ✅ Кнопка активується тільки коли номер введено правильно
- ✅ Показується індикатор завантаження "Відправка..."
- ✅ Перехід на екран введення коду

**В консолі сервера має з'явитися:**
```
==================================================
📱 SMS КОД ВЕРИФІКАЦІЇ
📞 Номер: +380981234567
🔑 Код: 654321
==================================================
```

#### 3.3. Введіть код верифікації
- Введіть 6-значний код з консолі сервера
- Натисніть "Далі"

**Що перевірити:**
- ✅ Валідація коду
- ✅ При невірному коді показується помилка
- ✅ При правильному коді - перехід на екран створення нового паролю

#### 3.4. Створіть новий пароль
- Введіть новий пароль
- Підтвердіть новий пароль
- Натисніть "Змінити пароль"

**Що перевірити:**
- ✅ Валідація паролю
- ✅ Перевірка співпадіння паролів
- ✅ Показується індикатор завантаження
- ✅ Успішний перехід на екран входу

#### 3.5. Увійдіть з новим паролем
- Введіть номер телефону
- Введіть **новий** пароль (той, що щойно створили)
- Натисніть "Увійти"

**Що перевірити:**
- ✅ Вхід успішний з новим паролем
- ✅ Старий пароль більше не працює

---

## 🔍 Перевірка в MongoDB

### Перегляд даних

```bash
# Підключення до MongoDB
mongosh mongodb://localhost:27017/smartcoach
# Або через MongoDB Compass
```

```javascript
// Перегляд користувачів
db.users.find().pretty();

// Перегляд кодів верифікації
db.verificationcodes.find().pretty();

// Перегляд refresh токенів
db.refreshtokens.find().pretty();
```

### Очищення тестових даних

```javascript
// Видалити тестового користувача
db.users.deleteOne({ phone: "+380981234567" });

// Очистити всі коди верифікації
db.verificationcodes.deleteMany({});

// Очистити всі refresh токени
db.refreshtokens.deleteMany({});
```

---

## ⚠️ Типові проблеми

### Проблема 1: "Користувач з таким номером телефону вже існує"

**Рішення:**
- Використайте інший номер телефону
- Або видаліть користувача з БД:
  ```javascript
  db.users.deleteOne({ phone: "+380981234567" });
  ```

### Проблема 2: "Перевищено ліміт SMS"

**Рішення:**
- Зачекайте 1 годину
- Або очистіть коди в БД:
  ```javascript
  db.verificationcodes.deleteMany({ phone: "+380981234567" });
  ```

### Проблема 3: "Невірний код верифікації"

**Рішення:**
- Перевірте код в консолі сервера
- Переконайтесь, що код не прострочений (10 хвилин)
- Спробуйте запросити новий код

### Проблема 4: "Network request failed"

**Рішення:**
- Перевірте, що сервер запущений
- Перевірте API URL (Android: `10.0.2.2:3000`, iOS: `localhost:3000`)
- Перевірте CORS налаштування

### Проблема 5: Пароль не зберігається

**Рішення:**
- Перевірте, що код верифікації був введений правильно
- Перевірте логи сервера на помилки
- Перевірте валідацію паролю (мінімум 8 символів, великі/малі літери, цифри)

---

## 📊 Чек-лист тестування

### Реєстрація
- [ ] Введення номера телефону
- [ ] Отримання SMS коду (перевірка консолі сервера)
- [ ] Введення коду верифікації
- [ ] Створення паролю
- [ ] Перехід на екран входу

### Вхід
- [ ] Введення номера телефону
- [ ] Введення паролю
- [ ] Успішний вхід
- [ ] Збереження токенів
- [ ] Перехід на головний екран

### Відновлення паролю
- [ ] Відкриття екрана "Забули пароль?"
- [ ] Введення номера телефону
- [ ] Отримання SMS коду (перевірка консолі сервера)
- [ ] Введення коду верифікації
- [ ] Створення нового паролю
- [ ] Вхід з новим паролем
- [ ] Старий пароль не працює

---

## 🎯 Очікувані результати

### Після реєстрації:
- ✅ Користувач створений в БД
- ✅ Пароль захешований
- ✅ `isVerified = true`

### Після входу:
- ✅ Токени згенеровані та збережені
- ✅ Refresh токен збережений в БД
- ✅ Перехід на головний екран

### Після відновлення паролю:
- ✅ Новий пароль захешований
- ✅ Старий пароль не працює
- ✅ Можна увійти з новим паролем

---

## 📝 Нотатки

- **SMS коди** виводяться тільки в консолі сервера (development режим)
- **Rate limiting**: максимум 3 SMS на годину, 10 SMS на добу
- **Термін дії коду**: 10 хвилин
- **Максимальна кількість спроб введення коду**: 3 спроби
- **Токени**: access token (15 хв), refresh token (7 днів)

---

**Успішного тестування! 🚀**

