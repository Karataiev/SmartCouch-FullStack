# Інструкція для тестування поточного прогресу

## Крок 1: Створення .env файлу

Скопіюй `.env.example` в `.env` та заповни необхідні змінні:

```bash
cd server
cp .env.example .env
```

**Мінімальні змінні для тестування:**

```env
NODE_ENV=development
PORT=3000
MONGODB_URI=mongodb://localhost:27017/smartcoach
JWT_SECRET=your-super-secret-key-minimum-32-characters-long-for-testing
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d
CORS_ORIGIN=http://localhost:3000
LOG_LEVEL=info
```

> **Примітка:** Для тестування без реальної БД можна використати MongoDB Atlas (безкоштовний) або локальний MongoDB.

## Крок 2: Перевірка компіляції TypeScript

```bash
npm run build
```

Якщо все ОК - побачиш папку `dist/` з скомпільованими файлами.

## Крок 3: Перевірка лінтера

```bash
npm run lint
```

Якщо є помилки - виправ їх або запусти:

```bash
npm run lint:fix
```

## Крок 4: Запуск сервера

### Варіант A: Development режим (з auto-reload)

```bash
npm run dev
```

### Варіант B: Production режим

```bash
npm run build
npm start
```

## Крок 5: Тестування endpoints

### Health Check

Відкрий браузер або використай curl:

```bash
curl http://localhost:3000/health
```

**Очікувана відповідь:**
```json
{
  "success": true,
  "message": "Server is running",
  "timestamp": "2024-11-19T12:00:00.000Z"
}
```

### Перевірка помилок (404)

```bash
curl http://localhost:3000/api/nonexistent
```

**Очікувана відповідь:**
```json
{
  "success": false,
  "message": "Маршрут GET /api/nonexistent не знайдено"
}
```

## Крок 6: Перевірка логів

При запуску сервера в консолі маєш побачити:

```
✅ Підключено до MongoDB
🚀 Сервер запущено на порту 3000
📝 Environment: development
```

## Що перевіряється:

✅ TypeScript компіляція  
✅ Лінтер (ESLint)  
✅ Express сервер запускається  
✅ Health check endpoint працює  
✅ MongoDB підключення (якщо БД доступна)  
✅ Middleware безпеки (helmet, CORS, rate limiting)  
✅ Обробка помилок (404, error handler)  
✅ Логування (Pino)  

## Можливі проблеми:

### Помилка: "MongoDB connection failed"
- Перевір, чи запущений MongoDB локально або чи правильний URI в `.env`
- Для тестування можна тимчасово закоментувати `connectDatabase()` в `app.ts`

### Помилка: "JWT_SECRET має бути мінімум 32 символи"
- Збільш довжину JWT_SECRET в `.env` до мінімум 32 символів

### Помилка: "Port 3000 already in use"
- Зміни PORT в `.env` на інший (наприклад, 3001)

