# ✅ Швидкий чек-лист для тестування на емуляторі

## 🚀 Швидкий старт (5 хвилин)

### 1. Перевірка .env файлу

Переконайтесь, що в `server/.env` є правильні налаштування CORS:

```env
CORS_ORIGIN=http://localhost:3000,http://10.0.2.2:3000,http://127.0.0.1:3000
```

**Якщо немає - додайте або оновіть цей рядок!**

### 2. Перевірка MongoDB

**Якщо використовуєте MongoDB Atlas** (як у вашому випадку):
- ✅ НЕ потрібно запускати локальний MongoDB
- ✅ Перевірте, що `MONGODB_URI` в `server/.env` правильний
- ✅ MongoDB Compass - це просто GUI, сервер підключиться автоматично

**Якщо використовуєте локальний MongoDB:**
```bash
# macOS
brew services start mongodb-community

# Перевірка
mongosh mongodb://localhost:27017/smartcoach
```

### 3. Запуск сервера

```bash
cd server
npm run dev
```

**Очікуваний вивід:**
```
🚀 Сервер запущено на порту 3000
📝 Environment: development
```

### 4. Перевірка сервера

```bash
curl http://localhost:3000/health
```

Має повернути: `{"success":true,"message":"Server is running",...}`

### 5. Запуск мобільного додатку

**Android:**
```bash
npm run android
```

**iOS:**
```bash
npm run ios
```

---

## 📱 Тестування відновлення паролю

### Крок 1: Забули пароль
1. Відкрити екран "Забули пароль?"
2. Ввести номер: `+38 098 628 69 74` (або інший)
3. Натиснути "Надіслати SMS"
4. **Перевірити консоль сервера** - там має з'явитися SMS код
5. Має відбутися перехід на екран введення коду

### Крок 2: Введення коду
1. Ввести код з консолі сервера (6 цифр)
2. Натиснути "Далі"
3. Має відбутися перехід на екран створення нового паролю

### Крок 3: Створення нового паролю
1. Ввести новий пароль (мінімум 8 символів, великі/малі літери, цифри)
2. Підтвердити пароль
3. Натиснути "Змінити пароль"
4. Має відбутися перехід на екран входу

---

## 🔍 Що перевіряти

### В консолі сервера:
- ✅ Запити приходять (логуються)
- ✅ SMS коди виводяться (development режим)
- ✅ Немає помилок CORS
- ✅ Немає помилок валідації

### В додатку:
- ✅ Немає помилок "Network request failed"
- ✅ Помилки відображаються коректно
- ✅ Loading стани працюють
- ✅ Навігація між екранами працює

### В MongoDB:
```bash
mongosh mongodb://localhost:27017/smartcoach

# Перевірка даних
db.users.find().pretty()
db.verificationcodes.find().pretty()
```

---

## ⚠️ Типові проблеми

### "Network request failed" на Android
**Рішення:** Переконайтесь, що `CORS_ORIGIN` містить `http://10.0.2.2:3000`

### CORS помилки
**Рішення:** 
1. Оновіть `CORS_ORIGIN` в `.env`
2. Перезапустіть сервер

### MongoDB не підключається

**Якщо використовуєте Atlas:**
- Перевірте `MONGODB_URI` в `.env`
- Перевірте IP whitelist в Atlas (0.0.0.0/0 для тестування)

**Якщо використовуєте локальний MongoDB:**
- `brew services start mongodb-community`

### SMS код не видно
**Рішення:** Перевірте консоль сервера (не додатку!)

---

## 📝 Нотатки

- SMS коди виводяться **тільки в консолі сервера** (development режим)
- Для Android використовується `10.0.2.2` замість `localhost`
- Для iOS використовується `localhost`
- Rate limiting може обмежувати кількість спроб

---

**Детальна інструкція:** [TESTING_SETUP_GUIDE.md](../TESTING_SETUP_GUIDE.md)

