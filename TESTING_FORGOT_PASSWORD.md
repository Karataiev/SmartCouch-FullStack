# 🧪 Інструкція для тестування відновлення паролю

## 📋 Передумови

1. **MongoDB** має бути запущений
2. **Сервер** має бути запущений на порту 3000
3. **Мобільний додаток** має бути запущений (iOS/Android)

---

## 🔧 Крок 1: Налаштування сервера

### 1.1. Перевірка .env файлу

Переконайся, що в `server/.env` є всі необхідні змінні:

```env
NODE_ENV=development
PORT=3000
MONGODB_URI=mongodb://localhost:27017/smartcoach
JWT_SECRET=your-super-secret-key-minimum-32-characters-long
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d
CORS_ORIGIN=http://localhost:3000
LOG_LEVEL=info
```

### 1.2. Запуск сервера

```bash
cd server
npm run dev
```

**Очікуваний вивід:**

```
🚀 Сервер запущено на порту 3000
📝 Environment: development
```

---

## 📱 Крок 2: Налаштування API URL для мобільного додатку

### Важливо: API URL для різних платформ

Для **iOS Simulator**:

- `http://localhost:3000` ✅ працює

Для **Android Emulator**:

- `http://localhost:3000` ❌ НЕ працює
- Використовуй `http://10.0.2.2:3000` ✅

Для **фізичного пристрою**:

- Використовуй IP адресу твого комп'ютера: `http://192.168.x.x:3000`
- Знайди IP: `ipconfig` (Windows) або `ifconfig` (macOS/Linux)

### Оновлення API_BASE_URL

В файлах:

- `src/screens/ForgotPasswordScreen.js`
- `src/screens/ForgotPasswordCodeScreen.js`
- `src/screens/ResetPasswordScreen.js`

Заміни:

```javascript
const API_BASE_URL = __DEV__
  ? 'http://localhost:3000' // Для iOS
  : 'https://your-production-api.com';
```

На (для Android):

```javascript
import {Platform} from 'react-native';

const API_BASE_URL = __DEV__
  ? Platform.OS === 'android'
    ? 'http://10.0.2.2:3000' // Android Emulator
    : 'http://localhost:3000' // iOS Simulator
  : 'https://your-production-api.com';
```

---

## 🧪 Крок 3: Тестування через cURL (опціонально)

### 3.1. Тестування ендпоінту forgot-password

```bash
curl -X POST http://localhost:3000/api/v1/auth/forgot-password \
  -H "Content-Type: application/json" \
  -d '{"phone": "+380501234567"}'
```

**Очікувана відповідь (якщо користувач існує):**

```json
{
  "success": true,
  "message": "Код верифікації відправлено на ваш номер телефону",
  "code": "123456"
}
```

**Примітка:** В development режимі код також виводиться в консоль сервера:

```
📱 SMS код для +380501234567: 123456
```

### 3.2. Тестування верифікації коду

```bash
curl -X POST http://localhost:3000/api/v1/auth/verify-code \
  -H "Content-Type: application/json" \
  -d '{"phone": "+380501234567", "code": "123456"}'
```

**Очікувана відповідь:**

```json
{
  "success": true,
  "message": "Код верифікації підтверджено",
  "verified": true
}
```

### 3.3. Тестування створення нового паролю

```bash
curl -X POST http://localhost:3000/api/v1/auth/create-password \
  -H "Content-Type: application/json" \
  -d '{
    "phone": "+380501234567",
    "password": "NewPassword123"
  }'
```

**Очікувана відповідь:**

```json
{
  "success": true,
  "message": "Пароль створено успішно",
  "tokens": {
    "access": "...",
    "refresh": "..."
  },
  "user": {...}
}
```

---

## 📱 Крок 4: Тестування через мобільний додаток

### 4.1. Запуск додатку

```bash
# iOS
npm run ios

# Android
npm run android
```

### 4.2. Покрокове тестування

#### Крок 1: Відкрий екран входу

- Запусти додаток
- Має відкритися `LoginScreen`

#### Крок 2: Натисни "Забули пароль?"

- Натисни на текст "Забули пароль?" внизу поля паролю
- Має відкритися `ForgotPasswordScreen`

#### Крок 3: Введи номер телефону

- Введи номер телефону (наприклад: `+38 050 123 45 67`)
- Натисни "Надіслати SMS"
- **Перевір:**
  - ✅ Кнопка активна тільки коли номер повний (17 символів)
  - ✅ Показується індикатор завантаження "Відправка..."
  - ✅ Якщо користувач існує - перехід на екран коду
  - ✅ Якщо помилка - показується повідомлення про помилку

#### Крок 4: Введи SMS код

- Має відкритися `ForgotPasswordCodeScreen`
- Введи 6-значний код з консолі сервера
- **Перевір:**
  - ✅ Кнопка "Далі" активна тільки коли всі 6 полів заповнені
  - ✅ Таймер показує час до можливості повторної відправки
  - ✅ Кнопка "Надіслати SMS з кодом" активна після закінчення таймера
  - ✅ При невірному коді показується помилка

#### Крок 5: Створи новий пароль

- Має відкритися `ResetPasswordScreen`
- Введи новий пароль (мінімум 8 символів, великі/малі літери, цифри)
- Повтори пароль
- **Перевір:**
  - ✅ Валідація паролю працює (показує помилки)
  - ✅ Перевірка співпадіння паролів
  - ✅ Після успішного збереження - перехід на `LoginScreen`

#### Крок 6: Увійди з новим паролем

- На екрані входу введи номер та новий пароль
- Перевір, що вхід працює

---

## 🔍 Крок 5: Перевірка помилок

### Можливі проблеми та рішення

#### Проблема 1: "Помилка підключення до сервера"

**Причини:**

- Сервер не запущений
- Неправильний API_BASE_URL
- Проблеми з мережею

**Рішення:**

1. Перевір, що сервер запущений: `curl http://localhost:3000/health`
2. Перевір API_BASE_URL в екранах
3. Для Android використовуй `10.0.2.2` замість `localhost`

#### Проблема 2: "Користувач не знайдений"

**Причина:** Користувача з таким номером немає в БД

**Рішення:**

1. Спочатку зареєструй користувача через `RegistrationScreen`
2. Або створи користувача вручну в БД

#### Проблема 3: "Невірний код верифікації"

**Причини:**

- Код прострочений (10 хвилин)
- Код введено невірно
- Перевищено кількість спроб (3 спроби)

**Рішення:**

1. Запроси новий код
2. Перевір код в консолі сервера
3. Введи код швидше (до закінчення терміну дії)

#### Проблема 4: CORS помилка

**Причина:** Сервер не дозволяє запити з мобільного додатку

**Рішення:**
Перевір `CORS_ORIGIN` в `.env` файлі сервера

---

## 📊 Крок 6: Перевірка логів

### Логи сервера

Під час тестування перевіряй консоль сервера:

```
📱 SMS код для +380501234567: 123456
💾 Код верифікації збережено в БД
✅ Код верифікації підтверджено
```

### Логи помилок

Якщо є помилки, вони будуть в консолі:

```
❌ Помилка відновлення паролю
```

---

## ✅ Чеклист тестування

- [ ] Сервер запущений та доступний
- [ ] MongoDB підключена
- [ ] API_BASE_URL налаштований правильно для платформи
- [ ] Користувач існує в БД (або створений через реєстрацію)
- [ ] Екран "Забули пароль?" відкривається з LoginScreen
- [ ] SMS код відправляється та відображається в консолі
- [ ] Введення коду працює
- [ ] Таймер повторної відправки працює
- [ ] Валідація паролю працює
- [ ] Збереження нового паролю працює
- [ ] Вхід з новим паролем працює
- [ ] Обробка помилок працює (неіснуючий користувач, невірний код, тощо)

---

## 🎯 Додаткові тести

### Тест 1: Rate Limiting

- Спробуй відправити більше 3 SMS за годину
- Має з'явитися помилка про перевищення ліміту

### Тест 2: Прострочений код

- Запроси код
- Зачекай 11 хвилин
- Спробуй ввести код
- Має з'явитися помилка "Код верифікації прострочений"

### Тест 3: Невірний код

- Введи невірний код 3 рази
- Має з'явитися помилка про перевищення спроб

### Тест 4: Навігація назад

- Перевір, що кнопка "Назад" працює на всіх екранах
- Перевір, що дані не втрачаються при поверненні

---

## 📝 Примітки

1. **Development режим:** SMS коди виводяться в консоль сервера та повертаються в API відповіді
2. **Production режим:** SMS коди відправляються через Twilio (якщо налаштовано)
3. **Безпека:** Для неіснуючих користувачів повертається успішна відповідь (захист від перевірки номерів)
