import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

export const ClientsComponent = () => {
  return (
    <View>
      <Text>ClientsComponent</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
